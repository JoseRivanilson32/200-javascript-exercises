// Declare a variable called "maxNumber", assing to it the maximum value a number can have in JavaScript, and print the value to the console.

let maximumNumber = Number.MAX_VALUE;
console.log(`maximum value: ${maximumNumber}`);

// Explanation:
// JavaScript has a maximum numeric value that can be represented, which is accessible through the static property Number.MAX_VALUE. Here, we assing this value to a variable called maxNumber, and then print it using console.log.