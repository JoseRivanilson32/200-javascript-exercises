// Declare two variables and assing boolean values to them. Use the logical operators AND, OR, and NOT to perform logical operations between the variables. Print the results.

let bool1 = true;
let bool2 = false;

console.log(`AND: ${bool1 && bool2}`);
console.log(`OR: ${bool1 || bool2}`);
console.log(`NOT: ${!bool2}`);